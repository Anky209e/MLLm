[project]
name = "mllm"
version = "0.1.0"
description = "Developing LLMs from scratch"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
  "jupyter",
  "matplotlib",
  "numpy",
  "pandas",
  "plotly",
  "scikit-learn",
  "tensorboard",
  "tensorflow",
  "tiktoken",
  "torch",
  "torchvision",
  "tqdm",
]
